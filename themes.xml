<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DarkLightTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DarkLightTheme" parent="Base.Theme.DarkLightTheme" />
    <!--Light mode -->
    <!-- Screen background if light mode on-->
    <style name="screen">
        <item name="android:background">#fff</item>
    </style>
    <!-- Text background if light mode on-->
    <style name="TEXT">
        <item name="android:textColor">#000</item>
    </style>
    <!--Button background if light mode on-->
    <style name="BUTTON">
        <item name="android:background">@android:color/holo_blue_bright</item>
        <item name="android:textColor">#fff</item>
    </style>
</resources>